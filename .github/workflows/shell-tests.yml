name: Shell Tests

on:
  push:
    branches:
      - main
      - work
  pull_request:

jobs:
  shell-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate scripts and run test suites
        run: bash tests/test_suite.sh
